# ▶️ 2026-07-24 КОНЕЦ ДНЯ — v0.9.0 ЗАПУШЕН, ПОЛНЫЙ ИТОГ: `knowledge_base/01_sessions/SESSION_2026-07-24_mvp_release_v0.9.md`

> Читать ТОТ файл целиком первым в новой сессии — там всё: что сделано/проверено, что НЕ сделано (macro-script
> pipeline, оплата, Deep Research формат, site deploy), 6 багов найдено+починено, security-находки, деплой-манифест.
> Ниже — предыдущий слой (в процессе дня), НЕ протух, но начинай с файла выше.

---

# ▶️ 2026-07-24 — MVP-РЕЛИЗ (текущий канон: `knowledge_base/01_sessions/MVP_RELEASE_PLAN_2026-07-24.md`)

> Всё, что НИЖЕ этого блока — контекст 15.07 (Блоки 0-3), НЕ протухло как справка, но текущая задача — 4 блокера MVP.
> Разбор кода по 4 блокерам (доказательно, curl+node, а не «на глаз»):

- **Блокер 1 — логин переживает перезапуск: ✅ СДЕЛАНО В КОДЕ (T2-проверено).** `_session` в `app/bridge.js`
  теперь персистится в `userData/session.json` (0600, не логируется), грузится на старте, чистится при
  sign-out. Файлы: `app/bridge.js` (+persist/load, startBridge 3-й арг), `app/main.js` (передаёт
  `app.getPath('userData')`). Тест (node, 2 процесса): write→0600 ✅, свежий процесс восстановил сессию ✅,
  sign-out удалил файл ✅. **Осталось founder'у (T4):** реальный логин → перезапуск app → всё ещё залогинен.
  ➕ **Ключ LLM тоже персистится** (founder одобрил 24.07, чтоб не вставлять заново при каждом рестарте):
  `userData/llm-key.enc`, ШИФРОВАН через Electron `safeStorage` (OS keychain, не plaintext; нет keychain →
  просто не персистим, ключ работает сессию). Грузится на старте → `/api/llm-status` показывает connected.
  Тест (mock safeStorage): enc-файл 0600 → свежий процесс восстановил → llm-status active:anthropic ✅.
  `main.js` передаёт `safeStorage` 4-м аргом. Регресс: `npm run check:bridge` 19/19 ✅.
- **Блокер 2 — файл в чате: цепочка УЖЕ СОБРАНА, бэк-половина LIVE-проверена.** Бэк шлёт `model_ready`
  {stl/step/obj_url} → мост `streamChat` ловит → `saveModelFiles` качает в `~/Documents/ORYND/projects/{sid}/output/`
  → шлёт `{type:files}` → рендер зажигает «Open files»/«Open in Fusion» (cad-thinking `RunProgress`).
  Curl по живому бэку: box→ok vol=4000, STEP 200/15.5KB, STL 200, OBJ 200 — скачивание работает.
  **Осталось:** прогон реальной сборки через UI (см. Блокер 4) чтобы увидеть, что кнопки рисуются. Правок в
  коде пока НЕ требуется (аудит 23.07 «фронт не рендерит» описывал более раннее состояние — с тех пор мост
  дописан). Если на реальном прогоне пусто — тогда точечный фронт-фикс.
- **Блокер 3 — MCP по правильной ссылке: ✅ адрес в исходнике** (`bridge.js:27 MCP_PUBLIC_URL=https://mcp.oryndai.com/mcp`).
  **➕ 24.07: ЛИЧНЫЕ долгоживущие токены построены (founder: «сто процентов личные токены всем, обязаловка»).**
  Механизм: НЕ общий секрет — таблица `mcp_tokens` (user_id→pat_… + Supabase refresh_token), `POST /mcp/token`
  (routers/mcp.py) минтит/возвращает токен юзеру по его реальному JWT; `_resolve_auth` Mechanism 3 на каждый
  MCP-вызов обменивает refresh_token на свежий access_token через САМУ Supabase → проверяет ТЕМ ЖЕ
  `verify_supabase_jwt`, что и остальные эндпоинты — новый путь доверия НЕ создан (identity/списание кредитов
  не продырявлены). Приложение (Settings→MCP) и сайт (account.html) оба минтят через этот endpoint, с fallback
  на старый 1ч JWT если минт не удался (никогда не показывают пусто).
  **Задеплоено на сервер, import-smoke+restart OK, live-проверено:** `/mcp/token` без auth→401,
  с мусорным Bearer→401 чисто (не 500), старые механизмы (`/mcp` tokenless→401) не задеты.
  **⛔ БЛОКЕР ДО ЖИВОГО ТЕСТА:** таблица `mcp_tokens` ещё не создана в Supabase — миграция
  `supabase/migrations/20260724000001_mcp_tokens.sql` ждёт founder'а (SQL Editor, как прошлые миграции).
  До миграции минт вернёт 503 (обработано мягко, не сырая ошибка) → оба фронта откатятся на 1ч-ссылку.
  **Сайт (account.html) правки НЕ задеплоены** — нужен push author=Sfnbinib → branch `static-v3`.
- **Блокер 4 — прогон тулов через UI на Anthropic-ключе: нужен founder** (Gemini free ловит 429). Бэк-путь
  тулов заранее проверен живьём (box→файлы отдаются). Founder гоняет в app на Anthropic-ключе → я смотрю логи
  → где пусто в чате, точечный фронт-фикс.

**Итог:** реальная правка кода = только Блокер 1 (сделан). 2 и 3 уже закрыты в коде; 2 и 4 = прогон founder'а через UI.

---

# ▶️ СТАРТ НОВОЙ СЕССИИ — ЧИТАТЬ ПЕРВЫМ (обновлено 15.07, ~17:00)

**Ты продолжаешь релиз-день. НЕ переспрашивай что делать — иди в задачу ниже.**

**✅ БЛОК 2 — ВЕСЬ КОД ЗАКРЫТ 15.07, кроме 2.6.** Сделано: 2.2 (изоляция ключей), 2.5 (Share),
2.7 (мелочи), 2.8 (Feedback), 2.9 (верификация). Разборы — в самих тасках ниже, там же 4 находки,
которые чинились по ходу (ключ жёг бы НАШ env-ключ · утёк бы в промпт · сырой JSON в чате при 401 ·
service-ключ поехал бы в DMG).

**⛔️ Осталось в Блоке 2 ровно одно — 2.6 (Project 0), и оно ЖДЁТ FOUNDER'А:**
1. **STL ×6** → `app/assets/project0/` (папку создал, там README с ТОЧНЫМИ именами файлов).
2. Твоё правило: **сначала визуал → ты смотришь → потом функционал.** Вслепую не делаю.

**✅ 2.10 АПГРЕЙД SEARCH — СДЕЛАН 15.07 (решение founder'а: Printables-only, минимально, мультиисточник после прода).**
🚨 **Главное открытие: поиск живёт на ОДНОМ источнике, и `THINGIVERSE_TOKEN` не работает НИГДЕ.**
Локально в `.env` строка пустая; на ПРОДЕ `curl /search` на «brake disc» и «gear» → 100% printables, Thingiverse
ноль (а у него тысячи шестерён). Founder был уверен, что токен работает — измерено, что нет. «52 источника» = 1.
**Причина «брелоков» была НЕ в источниках:** реальные детали (Brembo Brake Disc V2) лежали ниже топ-5, который мы
забирали. Брали 5 по best_match → показывали 5 брелоков.
**Что сделано** (`services/search/query_tools.py` НОВЫЙ + `printables.search_best` + `retrieval.py` Tier1=Printables):
- 3 англ-варианта запроса × 3 скоупа (весь сайт + `categoryId 51 Mechanical Parts` + `49 Tools`) → пул 74-147.
- 🚨 **`categoryId 93 Engineering` отдаёт 0 на ЛЮБОЙ запрос** — Codex советовал именно его, это сломало бы поиск.
  Не «чинить» обратно на 93. Рабочие — 51 и 49 (ID берутся из живого запроса `{ categories { id name } }`).
- Ранжирование: `√downloads + rating×10 + точная_фраза(+120) − аксессуар(−60) − декор(−500)`. Штраф аксессуара
  нужен: инструмент ДЛЯ тормоза (3.8k скачиваний) хоронил сам тормоз (461).
- RU: индекс англоязычный + **русский склоняется** → стемминг, непереведённая кириллица не едет в запрос.
- Codex подтвердил: у Thangs и GrabCAD публичного search API НЕТ → мультиисточник отложен обоснованно.
**Результат (живые прогоны):** «тормозной диск» 1 подстаканник → **SRAM Brake Disc + Brembo Brake Disc V2**;
«M8 hex bolt 30mm» 0 → 5 реальных; «найди мне модель кронштейнА для полки» → реальные кронштейны; 0.6-1.0s.
**Тесты:** `tests/test_search_quality.py` — 12, на моках (не краснеют от того, что Printables перетасовал выдачу).
Общий прогон: **312 passed, 1 pre-existing env-fail.**
⚠️ **Файлы в деплой 3.2 (НОВОЕ):** `orynd_core/services/search/query_tools.py` (**новый файл**),
`orynd_core/services/search/printables.py`, `orynd_core/agents/retrieval.py`, `orynd_core/models/schemas.py`
(+2 поля в Candidate: `download_count`, `rating`).
🟡 В roadmap (не сейчас): Thingiverse-токен перевыпустить (+2 млн моделей, код уже ждёт — Tier 2 подключён),
мультиисточник/harness, своя накопительная база (hook = `curated.py` Tier 0).

**Дальше по плану — БЛОК 3 (деплой/тесты). Там СТОП-ПРАВИЛО: сначала утверждение founder'а** (MCP-тесты
жгут его лимитированные кредиты — он должен заранее знать, что именно гоняем).

🚨🚨 **СТОП. ДЕПЛОЙ ЗАБЛОКИРОВАН. Сначала читать: `knowledge_base/02_architecture/SERVERS_MAP.md`**
(карта обеих машин по замерам 17.07) + `ANALYSIS_2026-07-16_two_servers_one_codebase.md` (почему так вышло).
**ВЫЯСНЕНО ЗАМЕРАМИ 17.07 — разделения серверов НЕ СУЩЕСТВУЕТ:**
• `api.oryndai.com` = **СТОКГОЛЬМ** (t3.micro, nginx 443 → uvicorn 8003). Туда смотрят И приложение
  (`bridge.js:13`), И сайт (`auth.js:6`). Там крутится ВЕСЬ `orynd_core` — биллинг + CAD + search + ai_model_4.
• **ВИРДЖИНИЯ (t3.small, 8765) ПРОСТАИВАЕТ** с 23 июня — 43 мин CPU за месяц, env только
  `PYTHONUNBUFFERED`, никто с ней не говорит. **Любой scp туда = деплой в пустоту.**
• Тяжёлый CAD крутится на **самой слабой** машине (1 vCPU) и на той же, через которую идут деньги →
  декомпозиция забирает ядро → **у другого юзера падает checkout**.
• 🚨 Наш `ANTHROPIC_API_KEY` стоит на Стокгольме, который обслуживает `/chat` → **мы платим за LLM юзеров**
  (против Блока 0). Вирджинии он не нужен вообще — ключ юзера едет в `X-LLM-*` (таск 2.2).
**Сначала разделение по `SERVERS_MAP.md` §6, потом деплой.** Команды ниже верны, но цель не та.
🚨 Команда `git -C orynd_core push origin master` из хендоффа — **НЕВЕРНАЯ** (отстаём на 4 коммита,
отлетит; с `--force` снесёт фиксы оплаты). Пуш сайта = `git subtree push --prefix=orynd_core back master`
из корня — founder должен подтвердить.

**🚀 ДЕПЛОЙ — все команды в одном месте: `knowledge_base/01_sessions/HANDOFF_2026-07-16_deploy_then_mcp.md`**
(3 места, Вирджиния в 2 захода: сначала search → проверка → потом гейт/списание → проверка).
**Сценарии тестов (10 шт, с промптами и ценой в сборках):** `knowledge_base/06_product/SCENARIOS_core_demo.md`.

**🚨 БЕЗОПАСНОСТЬ 16.07:** репо `orynd_cl_site_profe_back` **был публичным** с 29.06 и содержал весь
`orynd_core` (AI Model 4 — 17 файлов, services — 100, `_SYSTEM`-промпт). **Founder закрыл на приват.**
Причина: 29.06 внутри `orynd_core/` сделали `git init` + `git add .` + push ради доставки `billing.py` —
отдельной папки бэкенда сайта не было никогда. Сплит (тощая точка входа вместо 29 роутеров) — ПОСЛЕ релиза.

**Проверка (для всего Блока 2):** `ORYND_SKIP_KEY_VALIDATION=1 ORYND_DISABLE_AUTH_GATE=1
.venv/bin/python -m pytest tests -q` → сейчас **300 passed, 1 failed** (было 285: +9 тестов изоляции,
+6 feedback; `test_factory_defaults_to_mock_when_supabase_keys_missing` = pre-existing env-fail, признан
допустимым). Плюс `cd app && npm run check:bridge` → **19/19** (живой мост на порту 8901, не 8765).
`node --check` для bridge.js; `.jsx` он не берёт по расширению → копировать в `.js` в scratchpad.

**СТОП-ПРАВИЛО:** дойдёшь до Блока 3 (тесты/деплой) — **остановись и скажи founder'у**. Он утверждает
MCP-тесты заранее (что гоняем, под каким токеном, сколько сборок спишется — у него ЛИМИТИРОВАННЫЕ кредиты).

**Ждёт founder'а (не блокирует код):**
- ✅ **SQL применён founder'ом 15.07** (`20260715000002_starter_plan.sql`) — 🚨 БЛОКЕР STARTER ЗАКРЫТ.
  Проверено в живой БД: `subscriptions_plan_check` = `CHECK (plan = ANY (ARRAY['free','starter','pro','team']))`
  → оплата Starter теперь активирует подписку. `config`: free_generations=3, pro_generations=60,
  **starter_generations=20** ✅ (совпало с fallback в billing.py).
  ℹ️ Попутно видно в БД: `subscriptions_free_demos_left_nonnegative` = `CHECK (free_demos_left >= 0)` —
  баланс не уходит в минус на уровне БД (не только в коде). Хорошо для 4.5.
  ⚠️ Была опечатка в первой версии скрипта (`pg_get_constraintdef(con)` вместо `(con.oid)`) — исправлена
  и в файле миграции; транзакция откатилась целиком, БД не пострадала.
- ⬜ **SQL**: `supabase/migrations/20260715000001_feedback.sql` → таблица под 2.8.
- ⬜ **F1 STL ×6** → `dist_public/orynd_cad_bridge/app/assets/project0/` (блокирует ТОЛЬКО 2.6).
- ⬜ Заглушка `try.html` (косметика): мок с главной или его скрин.
- ⬜ Деплой — по готовности Блока 2 (список файлов в 3.2 ниже).

**Правила:** НЕ `git push` из корня монорепо · сайт → только ветка `static-v3`, автор `Sfnbinib` ·
приложение/бэкенд founder'а не рестартовать без спроса · формулировки founder'а не пересказывать.

---

# ACTIVE TASK — RELEASE DAY (3 блока)

> Один живой план. Блоки самодостаточны (свой контекст → таски → критерий закрытия),
> идти по порядку: 0 → 0.1 → 1 → 2 → 3 → 4. Прогресс отмечать здесь.
> 2026-07-15. Цель: релиз v1.0. Роли: код — Claude; пуши/деплой/оплаты — founder.
> ⚙️ АВТОМАТИЗАЦИЯ: ~70% плана Claude закрывает сам (код Блоков 0.1/1/2 + node-check/pytest + e2e через MCP).
> 2 чекпоинта founder'а: деплой сайта (static-v3) + деплой бэкендов (scp). Только founder: токен себе в
> Supabase, ревью визуала Project 0, Fusion-live (3.5), реальная оплата (Блок 4).

## Как обновляется каждый продукт (единственное, что надо знать про инфру)
- **Сайт**: локально `ORYND_Workspace/orynd_site_v3/` → коммит от автора **Sfnbinib** (SaveliTor Vercel
  блокирует) → `git push origin main:static-v3` (⚠️ только `static-v3`) → Vercel обновится САМ.
- **Бэкенд сайта** (аккаунты/оплаты, `billing.py`): код в монорепо `orynd_core/` → founder: subtree push
  в `orynd_cl_site_profe_back` + на серваке РУЧНОЙ `git pull && restart` (автодеплоя нет).
- **Extension-приложение**: репо `dist_public/orynd_cad_bridge/` → релиз = тег `v1.0.0` → GitHub Actions
  соберёт DMG/EXE → download.html уже смотрит `releases/latest`, подхватит сам.
- **Бэкенд extension** (3D/чат/оркестрация): код в том же `orynd_core/` → founder: scp файлов + restart
  (команда в Блоке 3). GitHub не подключён.
- **Supabase** — общий для всего, работаем через MCP (проект `dblquhnokgpavubobfoj`).
- 🚨 НИКОГДА `git push` из корня монорепо. Приложение/бэкенд founder'а не рестартовать без спроса.

---

# ✅ БЛОК 0 — МОДЕЛЬ ТОКЕНОВ (РЕШЕНО 15.07 — единый источник для сайта/enforcement)
> Читать только раздел «✅ РЕШЕНО» ниже — это истина. «Проблема» и «Что где происходит» = контекст-обоснование.

## Проблема (твоими словами, чтобы не исказить)
- «самая большая шутка — когда чувак создаёт приложение, которое не имеет сути: даёт ключевому продукту
  делать бесплатно, а доп-фичи платными. Это бессмысленно.» → монетизировать надо ЯДРО (получение детали),
  не периферию.
- «у инженеров не как у проггеров — здесь итог один всегда» (деталь). Разговор/уточнения — не результат.
- «если проект из 10 деталей — сделал 10, подписка закончилась — это некрасиво. Поэтому должен существовать
  unlimited в подписке 20. Причём реально unlimited, ну хорошо, 60 деталей.»
- локальный макрос + общение со своей нейронкой (BYOK) тратит ЕГО токены → нам не стоит → не ограничивать.
- через нас (доп-сервак: поиск хорошей модели, декомпозиция) = наши ресурсы → вот за это платит.
- должно быть ПРОСТО, чтобы юзер понимал, за что платит.

## Что где происходит (важно для монетизации)
- ЛОКАЛЬНО (машина юзера, его ключ): общение, уточняющие вопросы, генерация/правка макроса — нам НЕ стоит.
- ДОП-СЕРВАК (Вирджиния, наши ресурсы): поиск по 50 источникам, mesh-декомпозиция, + наша LLM-оркестрация
  если route=ORYND (на своём ключе LLM — за счёт юзера, у нас только серверный compute).

> (Ранняя рекомендация Claude была «BYOK = безлимит бесплатно, списываем только на route=ORYND» — founder
> ПОПРАВИЛ: подписка нужна всегда, списываем за каждую деталь. Актуально только «✅ РЕШЕНО» ниже.)

## ✅ РЕШЕНО (founder 15.07, после разбора MecAgent)
- **3 бесплатные детали (триал) → $10 Starter → $20 Pro.** «Три попытки — самый лучший вариант; дальше
  $10, первый месяц попользуешься и поймёшь, нужно ли». Free остаётся 3 (НЕ поднимаем до 5-10; конверсия
  через вау-эффект в первые минуты, не через щедрый free). MecAgent даёт 30/мес free — расточительно, мы
  идём другим путём.
- **Подписка нужна В ЛЮБОМ СЛУЧАЕ** (поправка founder'а): даже со своим ключом без подписки не работает —
  данные не уходят на доп-сервак. BYOK/MCP = способы управления (фичи), НЕ ценовые рычаги и НЕ бесплатный обход.
- **Макросы — ПЛАТНЫЕ (в подписке), НЕ бесплатные.** Технически макрос можно отдать бесплатно (локальный,
  нам не стоит), НО (founder): «чуваки обнаглеют… а потом скажут нахрена макрос, я свой cloud юзаю». Макрос —
  ключевой развиваемый элемент → в платную подписку, с объяснением «макрос много даёт, активно развиваем».
- **Единица = деталь, любым способом** (макрос/поиск/декомпозиция). Разговор, уточняющие вопросы, правки —
  бесплатно. Списание — на каждую построенную деталь (не только route=ORYND — см. поправку про подписку).
- **Дубликаты = не перерасход** (founder): «5 колёс + 5 тормозных = один поиск = 2 токена, не 10». Оркестратор
  должен распознавать идентичные элементы. → правило в 2.3 + roadmap (умный подсчёт).
- **Pro-кап = 60** (founder твёрдо: 150 много; инженер в среднем ~2 детали/день; MecAgent показывает 100,
  но мы осознанно 60 на бету — кто хочет больше, пока второй аккаунт). config `pro_generations=60`, показываем «Unlimited».
- **Starter = 15** (founder: hobbyist ~1 деталь/2 дня; философия «не давать Starter достаточно → гнать на
  Pro», как Claude vs ChatGPT). Вилка 15-25, взяли нижнюю; поднять легко (1 строка Supabase). На сайте не озвучиваем.
- **В приложении показывать остаток** (founder, важно): сколько деталей осталось по подписке (как Anthropic).
  Возможно Starter показывает число, Pro — «Unlimited». Счётчик 2.4 это делает. На САЙТЕ числа не пишем.
- 🟡 На будущее (НЕ v1): бонус +5 деталей после N дней использования (лояльность); периодический сброс
  лимитов в зависимости от числа юзеров — смотреть по ситуации. Идея «upgrade-переход Pro» founder отверг («бред»).

---

# 🔍 БЛОК 0.1 — ПРОВЕРКА РЕАЛЬНОСТИ ЯДРА (founder запереживал «есть ли это вообще»)
Founder 15.07: описывал концепцию словами, но не уверен, что она реально реализована. Проверить (это АУДИТ
кода, не стройка — понять что есть, что нет; чего нет и хотим → roadmap).
- ✅ 0.1a **Decompose = STEP или STL?** ПРОВЕРЕНО 15.07: выход STEP из параметрических примитивов
  (manual_decompose.py:13 «CAD bridge строит STEP»; primitives box/cylinder/sphere/cone/torus + отверстия
  → CoreOps → STEP). **Размеры менять МОЖНО** (правишь параметр примитива, пересобираешь). Оговорка:
  параметрично настолько, насколько чисто меш раскладывается (инженерные детали ок, органика частично).
  → Это КЛЮЧЕВОЙ дифференциатор (STL→редактируемый STEP), в отличие от MecAgent. Годно для маркетинга.
- ✅ 0.1b **Reverse-engineering инструкция в промптах? ОТВЕТ: НЕТ, не зашита.** Проверено 15.07.
  • `cad.py` — промпта НЕТ ВООБЩЕ: CADAgent = чистый исполнитель (CoreOps JSON → валидация → CadQuery).
    Инструкции модели там быть и не может — правильно по архитектуре («LLM никогда не исполняет CAD»).
  • `workspace.py` `_SYSTEM` (:197-261) — единственный промпт. Про reverse-eng там ТОЛЬКО две строки:
    :211 `analyze_mesh — REVERSE ENGINEER a mesh file (STL/OBJ): decompose → features → CoreOps JSON`
    (описание инструмента) и :229 роутинг-правило. **Методологии-последовательности нет.**
  • Для сравнения: `SEARCH QUERY METHODOLOGY` (:217-225) расписана детально (3-5 вариантов, general→
    specific). Для построения детали есть ОДНА строка :231 «ALWAYS: CreateSketch → Extrude → add
    features» + 1 пример. Твоей последовательности («изучить из чего состоит → окружность → отверстия»)
    как отдельного блока — нет.
  • ⚠️ Важный нюанс: reverse-eng МЕША делается КОДОМ, не промптом — AI Model 4 (`fitters/box_fitter.py`,
    `cylinder_fitter.py`, `torus_fitter.py`, `pass2_rebuild.py` → `cad_translator.py`). Там детермини-
    рованный подбор примитивов, промпт на него не влияет. Т.е. дописывать промпт имеет смысл для пути
    «построй деталь по описанию/фото», НЕ для analyze_mesh.
  → 🟡 РЕШЕНИЕ FOUNDER'У: дописать в `_SYSTEM` блок `PART DECOMPOSITION METHODOLOGY` (~15 строк, твоя
    последовательность: из чего состоит → базовое тело → оси/окружности → отверстия → фаски). Дёшево,
    workspace.py и так в списке scp на деплой (3.2). Риск: меняет поведение модели прямо перед e2e.
- ✅ 0.1c **Сборка-оркестрация: ОТВЕТ: НЕТ, не реализована** (как и предполагалось). Проверено 15.07.
  • У агента **7 инструментов** (workspace.py TOOLS) — assembly-инструмента среди них НЕТ.
  • **CoreOps = 11 операций** (schemas.py OP_REGISTRY: CreateSketch, Extrude, Cut, CutHole, CutSlot,
    Fillet, Chamfer, Revolve, Loft, Boolean, Mirror). Ни одной assembly-команды: нет mate/align/joint/
    constraint, и даже нет Transform/Move/Rotate. Позиционировать тело можно только КОСВЕННО — строя
    его сразу на месте: `CreateSketch.plane` + `offset` (Z-смещение) + `center` у rect/circle. Осевого
    выравнивания «состыкуй A с B» на уровне схемы не существует.
  • **Fusion-аддин joints не умеет** (grep по `ORYNDFusion360AddIn.py` — 0 совпадений joint/assembly/
    occurrence). Т.е. даже если агент вычислит координаты, применить Fusion-джойнт сейчас нечем.
  • Что ЕСТЬ и легко спутать: `agents/ai_model_4/assembly_export.py` — это НЕ сборка узла, а ЭКСПОРТ
    результата декомпозиции (один меш → N примитивов → part_01.step, part_02.step + full.step union).
    Обратная задача: разбирает одну деталь, а не собирает узел из разных.
  → В roadmap как приоритет макро-апгрейда: нужны (1) CoreOps-операции Transform/Mate, (2) assembly-
    инструмент агенту, (3) joints в Fusion-аддине. Это не «дописать промпт», это слой.
- ⬜ 0.1d **Assist (real-time сборка) — протестировать в e2e** (Блок 3.3): добавил детали → авто-сборка;
  «соедини колесо с распредвалом» → наш AI собирает. Founder просил напомнить, что это тестируем в одном
  из блоков — теперь явно в 3.3. Если не работает → не блокер релиза, в roadmap.
  ⚠️ ПРОГНОЗ по итогам 0.1c: скорее всего НЕ сработает — assembly-команд нет ни в CoreOps, ни у агента,
  ни в аддине. Ожидаемое поведение: модель либо откажется, либо построит тела рядом «на глаз» через
  offset/center. В 3.3 тестируем чтобы зафиксировать факт, а не чтобы починить.

---

# БЛОК 1 — Supabase + Сайт ⬜ (Supabase-часть ✅ сделана 15.07)

## 📖 КОНЦЕПЦИЯ (что мы вообще меняем и зачем — читать первым)
**Откуда и куда.** Раньше в голове была модель «подписка = токены / часовые лимиты». Красивая, но чисто
реализовать пока не получается. Поэтому на релиз берём модель попроще и понятную юзеру:
**подписка = кредиты = генерации** (1 успешная генерация детали = 1 кредит).

**Три тарифа (РЕШЕНО, Блок 0):**
| Тариф | Цена | Что даёт (внутри, на сайте НЕ озвучиваем) | Как показываем на сайте |
|-------|------|-------------------|--------------------------|
| Free | $0 | 3 детали-триал (`free_demos_left`) | «Start free» |
| Starter | $9.99/мес | 15 деталей/мес (`starter_generations`) | «For hobbyists» (без числа) |
| Pro | $20/мес | 60 деталей/мес = кап (`pro_generations`) | **«Unlimited»** (founder: не пишем число; fine-print «fair-use during beta») |

**Зачем именно так (логика воронки).** Free = 3 детали специально мало: юзер за ~одну сессию их тратит,
понимает ценность → конвертируется. **Списываем за КАЖДУЮ построенную деталь любым способом** (макрос/
поиск/декомпозиция, вкл. BYOK) — подписка нужна всегда, без неё доп-сервак не обрабатывает. Разговор,
уточняющие вопросы, правки = бесплатно (не штрафуем за итерацию). Pro показываем «Unlimited» (кап 60
почти никто не упрётся, продаёт лучше; MecAgent за $16 даёт 100 — наш «unlimited» должен быть выше).

**Как три поверхности связаны (единый источник правды = Supabase):**
```
                    ┌────────── Supabase ──────────┐
                    │ config: цены + display-лейблы │◄──anon read──┐
                    │ subscriptions: баланс юзера    │              │
                    │ spend_generation(): списание   │              │
                    └───────────┬──────────┬─────────┘              │
        service_role (backend)  │          │  service_role          │
   ┌────────────────────────────┘          └──────────────┐        │
   ▼                                                        ▼        │
САЙТ-бэкенд (Стокгольм, billing.py)              EXTENSION-бэкенд (Вирджиния, chat.py)
 • checkout: цена плана из config                 • перед КАЖДОЙ построенной деталью:
 • CryptoBot invoice → webhook → +кредиты           spend_generation(user) → нет → стоп
   в subscriptions                                • счётчик в приложении = баланс
   ▲                                                        ▲
   │ POST /api/billing/checkout {plan}                      │ GET /api/billing/me
   │                                                        │
САЙТ (статика, читает цены anon-ключом)          ПРИЛОЖЕНИЕ (extension, Блок 2)
```
Смысл: цену поменял в Supabase `config` → и сайт (через pricing.js), и бэкенд (billing.py читает config)
берут новую цифру. Ничего не хардкодим в двух местах.

**✅ Starter vs Pro (решено):** Starter 15/мес «For hobbyists», Pro «Unlimited» (кап 60). Число нигде
не озвучиваем. Разница на карточках = «for hobbyists» vs «unlimited, for makers who ship».

**Закрыт БЛОК когда:** сайт на проде показывает 3 тарифа с ценами из Supabase, checkout создаёт CryptoBot-
инвойс на $9.99 для starter, /try открывается.

## ✅ SUPABASE — СДЕЛАНО 15.07 (это и есть аналитика: к чему подключается фронт/бэкенд)
Проект `dblquhnokgpavubobfoj`. Применено через MCP, проверено, security-адвайзор чистый по этим объектам.
- ✅ **config** (RLS on; anon читает ТОЛЬКО price+display ключи — политика `config_public_price_read`):
  | key | value | кто читает |
  |-----|-------|-----------|
  | `pro_monthly_usd` | `20` | сайт (anon) + billing.py |
  | `starter_monthly_usd` | `9.99` | сайт (anon) + billing.py |
  | `pro_display` | `Unlimited` | сайт (anon) |
  | `starter_display` | `For hobbyists` | сайт (anon) |
  | `free_generations` | `3` | backend (service_role) |
  | `starter_generations` | `20` ✅ (founder применил SQL 15.07) | backend |
  | `pro_generations` | `60` | backend (кап «Unlimited») |
  | `founder_usd_amount` | `25` | НЕ anon (приватный) |
- ✅ **subscriptions.free_demos_left**: default 1→**3**; все 5 free-юзеров приведены к `free_demos_left=3,
  credits_balance=0` (чистая free-модель). Founder может UPDATE'ить баланс любому руками в любой момент.
- ✅ **RPC `spend_generation(p_user uuid)`** → jsonb: атомарно (FOR UPDATE) списывает 1 сначала из
  free_demos_left, потом из credits_balance (+credits_spent_total), пишет в `credit_ledger`; при нуле →
  `{ok:false, reason:'no_credits'}`. **EXECUTE только у service_role** (backend); anon/authenticated
  отозваны (иначе аноним мог бы слить чужие кредиты — дыру нашли адвайзором и закрыли).
- ✅ **credit_ledger** — журнал списаний (delta/reason/ref_type), заполняется функцией.
- ⚠️ Найдено, НЕ мои, в roadmap: `ensure_subscription_row` (pre-existing) исполним anon с той же
  проблемой; leaked-password protection выключена. Не блокеры релиза.

## Осталось в БЛОКЕ 1 — Бэкенд сайта + Фронт
- ✅ 1.3 `billing.py`: `Plan = Literal["starter","pro","team"]`; `_plan_config` стал **async** — цены и
      кап читаются из Supabase `config` (`starter_monthly_usd`/`pro_monthly_usd`/`starter_generations`/
      `pro_generations`), env = fallback, любая ошибка чтения → env (checkout не падает); starter 9.99/**20**,
      pro 20/60; `/api/billing/me` → +`free_demos_left` +`plan_max`. Тесты: 14 passed.
      🚨 **Попутно найдено 3 бага, которые ломали релиз — исправлены:**
      1. `users.py:72 set_plan` не принимал `"starter"` → ValueError → **юзер платит, план не активируется**.
      2. `users.py ensure_subscription` выдавал новым юзерам `credits_balance=PLAN_FREE_CREDITS(=100)` →
         каждый регистрант получал **100 кредитов + 3 демо = 103 сборки**, модель монетизации мертва.
         → теперь `credits_balance=0`, free живёт на `free_demos_left` (дефолт 3 владеет БД).
         Заодно убран мёртвый env `PLAN_FREE_CREDITS` — стоял бы на серваке и сломал бы снова.
      3. `billing.py _parse_order_id` отбрасывал план `starter` → **webhook по оплате Starter падал 400,
         деньги приходят, кредиты нет**.
- ✅ 1.4 `js/pricing.js` (новый, 66 строк) + `getSb` экспортирован из auth.js. Читает 4 ключа из `config`,
      подставляет в `[data-price=…]`/`[data-display=…]`, HTML = fallback при любой ошибке; шлёт событие
      `orynd:pricing` (checkout слушает). Подключён на index/checkout/download. node --check ✅.
- ✅ 1.5 index.html: баннер → «Starter $9.99/mo · Pro $20/mo ~~$29~~»; pricing-grid → **3 карточки**
      (grid 1fr 1fr 1fr, max-width 900→1080, mobile column уже был); Pro показывает «Unlimited» + сноска
      «* Fair-use during beta»; JSON-LD → **AggregateOffer** (lowPrice 9.99 / highPrice 20 / 3 offers), парсится.
      ⚠️ Бейдж Pro был свёрстан под ОДНУ цифру (120px) — «Unlimited» (201px) ломал раскладку → добавлен
      вариант `.num.word` (46px) + `.word-badge` (вертикальный стек). Проверено в браузере: 3×347px, стек
      Unlimited→builds→$20/month, горизонтального скролла нет.
- ✅ 1.6 checkout.html: тир Starter добавлен; free «100 AI credits»→«3 free builds», pro «1,500 AI credits»→
      «Unlimited builds». 🚨 **Ещё 2 бага исправлены:** (а) `parseInt("9.99")=9` → Starter биллился бы как
      **$9**; (б) выбор тира в модалке менял ТОЛЬКО текст — `loadInvoice()` брал план из URL → **юзер
      выбирает Starter, а инвойс уходит на Pro $20**. Теперь `selectedPlan` владеет и сводкой, и инвойсом;
      план из URL синхронизирует карточки на загрузке; тир `free` больше не дёргает checkout (был бы 422).
- ✅ 1.7 Тексты. ⚠️ Конфликт оказался ШИРЕ, чем в плане: первый проход нашёл 5 мест, расширенная сверка
      (по `watermark|AI credits|builds a day|1 day|free trial`) нашла **ещё 8**. Итог — сайт больше нигде
      не обещает того, чего код не делает:
      • index: hero/marquee/step-03/FAQ/финальная плашка → «3 free builds»; `fc-trial` «1 day free»→«3 free
        builds»; FAQ про аккаунт переписан (аккаунт НУЖЕН — счётчик привязан к юзеру, старый текст обещал
        сборку без регистрации, а enforcement требует JWT).
      • login.html: **чёрный бейдж СОХРАНЁН** (founder: «охуенный бейдж») — цифра 3 подошла под новую
        модель, поменялся только смысл: «3 / builds free / NO CARD · FULL EXPORT»; фича-лист «Unlimited
        builds» → «Any input» (на странице, обещающей 3 сборки, «unlimited» противоречил сам себе).
      • download.html: «**1 day** free trial» → «3 builds free»; футер «3 demo builds/day · watermarked
        exports» → «3 free builds · full export, no watermark».
      • user-guide.html: раздел Export (watermark) переписан; FAQ «My export is watermarked» → «I've run
        out of builds» (watermark'а в коде нет — вопрос был про несуществующую фичу).
      • checkout.html: «1,500 AI credits / month» → список «что входит» теперь зависит от выбранного плана
        (`INCL_BUILDS`); у Starter число НЕ озвучивается («More builds / month · for hobbyists»).
      • account.html: лейбл «AI credits» → «Builds left».
      • **terms.html (юридическое!)**: пункт «Free trials convert to paid unless cancelled before the trial
        ends» — прямая ложь: триала по времени нет, автоконверсии нет, CryptoBot выставляет разовые
        инвойсы. Переписан: free не конвертируется и не списывает; «Renewals» → «в бете планы покупаются
        за период и НЕ автопродлеваются». Это же снимает риск по 4.3/4.5.
- ✅ 1.8 account.html: prices `{free:'',starter:'· $9.99/mo',pro:'· $20/mo'}` (team уже не было в UI);
      planMax больше НЕ хардкод — приходит из `/api/billing/me.plan_max` (бэкенд читает config); счётчик =
      одно число `free_demos_left + credits_balance`; **Pro → «Unlimited»** (кап 60 не светим).
- ✅ 1.9 `try.html` (новый): hero «See what they made with ORYND» + Download (реальные GitHub-релизы,
      определяет ОС) + «Start free — 3 builds»; `utm_source` → localStorage `orynd-utm` → дописывается к
      ссылке на register. Проверено в браузере: utm сохраняется, ссылка `register.html?utm_source=share`.
- ✅ 1.11 (добавлено) Миграция `supabase/migrations/20260715000001_feedback.sql` — таблица `feedback` под
      Блок 2.8 (RLS on, политик нет → только service_role). Founder применяет через SQL Editor.
      ⚠️ **Дрейф схемы:** `config`, `spend_generation`, `credit_ledger`, дефолт `free_demos_left=3`
      сделаны 15.07 через MCP и в репо миграций НЕТ — схема живёт только в живой БД. Не блокер релиза,
      но пересоздать проект с нуля сейчас нельзя. → в roadmap: выгрузить их в миграцию.
- ⬜ 1.10 **Founder**: коммит (автор Sfnbinib) → `git push origin main:static-v3` → Vercel Ready;
      subtree push бэкенда сайта + на серваке pull/restart + env из 1.3. Проверка: checkout starter → $9.99.
      🚨 CryptoBot: 7 инвойсов pending / 0 оплат за историю → проверить webhook URL в настройках бота (реальная оплата — Блок 4.3).

# БЛОК 2 — Extension (приложение = локальный фронт+бэк на машине юзера + доп-сервак) ⬜
**Устройство (словами founder'а):** Extension — это локальное приложение (фронт+бэк вместе на машине
юзера); плюс ОДИН ДОП-СЕРВАК (Вирджиния) — внешние вычислительные мощности, куда уходят тяжёлые вещи
(поиск по источникам, декомпозиция), а лёгкое (макрос) делается локально. Как обычный API: вычисления
на сервере, и надо чтобы **сессии 10 разных локальных клиентов на доп-серваке не путались между собой**
(чтобы запрос одного не ушёл другому).
**Цель:** доп-сервак не путает сессии/ключи разных клиентов + concurrency; enforcement-вызов списания;
Share; Project 0; счётчик; feedback; добить BYOK.
**Закрыт когда:** node --check + pytest зелёные, Project 0 проходит офлайн, share копирует ссылку,
два curl с разными юзерами к доп-серваку не пересекаются (истории/ключи раздельны).

## Контекст (проверено)
- Незакоммичено (осознанно, коммит в Блоке 3): orynd_core (BYOK Фаза A: routes.py/chat.py/workspace.py)
  + extension (6 файлов: settings-v2, components, app, empty-states, bridge.js — мультипровайдер + счётчик).
- ⚠️ ДЫРА, которую чиним («сессии путаются» на доп-серваке): (а) панель не шлёт session_id → мост дефолтит
  'cad-bridge' (bridge.js:467) → на серваке истории всех юзеров в одной куче; (б) /llm/key пишет ключ в
  общий os.environ процесса (chat.py:~1152) → ключ одного виден/перетёрт другим. Читатели env: chat.py:101
  (_make_provider), workspace.py:1018+ (_formation_provider). Ниже 2.1/2.2 это закрывают.
- Онбординг: 6 фикс-промптов `F1_PARTS` + чекофф по точному совпадению (cad-app.jsx:489).
- Ран-блок: send() cad-app.jsx:427+, финализация :480 (filesDir, elapsedMs), LABEL шагов :41.

## Таски
- ✅ 2.1 Изоляция-сессии (15.07). `userFromSession` теперь отдаёт `id`; добавлен `clientId()` (стабильный
      uuid в localStorage на случай анонимной сессии); `chatSessionId()` = `'cad-' + (user.id || clientId())`;
      `send()` кладёт `session_id` в тело → мост больше не дефолтит `'cad-bridge'`. node --check ✅.
- ✅ 2.4 Остаток + подписка ВИДНЫ (15.07).
      • bridge: новый `GET /api/credits` → форвард на `/api/billing/me` с JWT юзера → нормализует в
        `{ok, plan, builds_left, credits_balance, free_demos_left, plan_max}`. Fail-soft: без авторизации/
        при недоступном бэкенде отдаёт `{ok:false}`, панель не падает.
      • cad-app: `refreshBilling()` тянет реальные данные при входе и **после каждого turn'а, который мог
        списать**. ⚠️ Отклонился от буквы плана осознанно: вместо локального декремента (`creditsMock - 1`)
        делаю **refetch** — только Supabase знает, что реально списалось (разговор не стоит ничего, дубликаты
        могут стоить меньше). Локальный минус врал бы при первом же несписании. `creditsMock` остался ТОЛЬКО
        как офлайн-кэш последнего known-значения.
      • **План юзера больше не фейк**: из `userFromSession` убран хардкод `plan:'Free'`, план приходит из
        `/api/billing/me`. Живое меню аккаунта (cad-components.jsx:58) уже рендерит `user.plan` → теперь
        показывает правду. Pro → «Unlimited», остальные → число.
      • 🚨 Найден баг в шапке: чип остатка рендерился под `Number.isFinite(credits)` → для Pro со строкой
        «Unlimited» счётчик **исчез бы совсем**. Плюс тексты были от мёртвой модели («Free builds remaining
        **today**», «**Daily** free builds used up»). Переписано: 'Unlimited' | «N builds left» | при 0 →
        «No builds left — upgrade at oryndai.com».
      • ℹ️ Уточнение к плану: зашитые «Pro · trial» (cad-auth.jsx:146) и «PRO · trial» (cad-dashboard.jsx:139)
        лежат в компонентах, которые в живом приложении **НЕ монтируются** (cad-app берёт из cad-dashboard
        только `PaneTabs`; `AUTH.HeaderWithUser`/`AccountMenu` не используются). Живая шапка — `Header` в
        cad-components.jsx. Фейки в галерейных компонентах не трогал (не на пути юзера) — в roadmap на чистку.
- ✅ 2.2 **Изоляция-ключи ✅ СДЕЛАНО (15.07).** Все 7 правок + тесты. **294 passed, 1 pre-existing env-fail**
      (было 285 → +9 тестов на сам контракт изоляции). `node --check` bridge.js ✅, cad-settings-v2.jsx ✅.
      • Ключ больше НЕ оседает на серваке: `/llm/key` только пингует вендора (verdict + live-список
        моделей) и **ничего не пишет** — ни `os.environ[KEY]`, ни `os.environ[FORMATION_MODEL]`.
      • Ключ живёт в памяти моста (`_llm` в bridge.js, машина юзера) и едет заголовками
        `X-LLM-Key/Provider/Model` на каждый /chat; `/chat` кладёт их в `req.context.llm_keys` →
        `_stream` **сразу вынимает** и передаёт в `_make_provider`/`ctx.extra["llm_keys"]`.
      • Общий хелпер `routes.key_for/model_for/keys_from_headers` — один источник правила «чей ключ».
        Читатели env закрыты все три: chat.py:101, workspace.py:1044 (formation) и **:691 (клиент
        tool-loop'а — он брал env напрямую в обход провайдера)**.
      🚨 **Отклонение от буквы плана (осознанное, по твоим же словам) — ключ из запроса ГЛУШИТ наш env
        для ДРУГИХ провайдеров.** План говорил «ключ из запроса имеет приоритет над `ANTHROPIC_API_KEY`».
        Буквально по-провайдерно: юзер с Gemini-ключом на route=ORYND → `key_for("anthropic")` нашёл бы
        НАШ env-ключ на Вирджинии и каскад ушёл бы на Claude → **юзер со своим ключом жёг бы наши деньги**
        (против Блока 0: «на своём ключе LLM — за счёт юзера, у нас только серверный compute»). Сделано:
        принёс ключ → весь turn на нём; env — только когда в запросе не пришло ничего.
      🚨 **Найдено попутно: ключ утёк бы В ПРОМПТ.** `_context_note` (workspace.py:945) сериализует ЛЮБОЕ
        незнакомое поле `context` в системную заметку модели (`other: {...}`). Положи мы ключ в
        `req.context` буквально — он бы уехал в чужую LLM (и в её логи). Поэтому `llm_keys` вынимается из
        контекста до всего остального и живёт в `ctx.extra` (в промпт/историю не попадает — тест есть).
      ⚠️ **Ключ НЕ переживает перезапуск приложения** (память процесса — так в плане и написано). Юзер
        вставляет ключ заново при каждом старте. Персист (safeStorage/keychain) — в roadmap, не к релизу.
      ⚠️ **Задело Workspace-прототип** (`design/…/workspace/settings.jsx` — общий бэкенд): «вставил ключ в
        UI → бэкенд его держит» больше не работает нигде, включая прототип. На локалке он живёт на `.env`
        (env = fallback локальной машины) → не блокер. Прототип не трогал (твоё правило).
      ⚠️ Остаток (не 2.2, в roadmap): `KEY_VERIFIED` в routes.py — тоже process-global (вердикт, не
        секрет). Для Extension обезврежен: мост отдаёт свой вердикт в `/api/llm-status`.
      ⚠️ Файлы в деплой 3.2: `services/llm/routes.py`, `routers/chat.py`, `agents/workspace.py` — **все три
        уже в списке 3.2**, новых файлов на сервак не добавилось.
- 🔄 2.3 **ЧАСТЬ 1 — AUTH-ГЕЙТ ✅ СДЕЛАН (15.07, founder дал «да»)**. Без него enforcement был бы декорацией.
      **Что было:** middleware `_auth_visibility` в main.py **только логировал** («Pure logging — never
      changes the response») → `curl -X POST https://api.oryndai.com/chat -d '{"message":"..."}'` **без
      токена работал** и жёг наш compute/LLM. Токен требовали 3 роутера из 29 (auth/billing/credits).
      `/chat` резолвил `optional_user`, `user_id` дефолтился в `"anonymous"` (chat.py:48) → юзер с 0 сборок
      просто убирал заголовок Authorization и строил бесплатно вечно. Это ровно риск п.4.5.
      **Что стало:** `security.py` → `GATED_PREFIXES = (/chat, /cad, /mesh, /research)` (то, что тратит
      наши деньги) + `auth_gate_enabled()`; middleware переименован в `_auth_gate` и **реально отбивает 401**,
      причём **верифицирует подпись JWT**, а не наличие заголовка (иначе любая строка после «Bearer»
      проходила бы, и chat.py снова падал бы в "anonymous"). Остальные роутеры (library/sources/skills) —
      по-прежнему log-only: они не тратят деньги за вызов, и правило founder'а «флипать только когда логи
      чистые» соблюдено.
      **Fail-closed:** гейт ВКЛЮЧЁН по умолчанию; `ORYND_DISABLE_AUTH_GATE=1` — только для тестов (они
      дёргают эндпоинты напрямую и не могут выпустить настоящий Supabase-JWT); **в проде
      (`BACKEND_ENV=production`) флаг выключения ИГНОРИРУЕТСЯ** — случайная env на серваке не может открыть
      бэкенд обратно.
      **Проверено реальными запросами (не на словах):** без токена /chat,/cad,/mesh,/research → 401 ✅;
      фальшивый Bearer → 401 ✅; /health,/llm/status → 200 ✅ (публичное не сломал); prod+DISABLE=1 → 401 ✅.
      **Тесты: 285 passed, 1 failed** — упавший `test_factory_defaults_to_mock_when_supabase_keys_missing`
      = тот самый pre-existing env-fail library, который план заранее признал допустимым.
      ⚠️ **Гейт живёт в бэкенде — пока не задеплоен на Вирджинию, в проде его НЕТ.** Файлы в 3.2: добавить
      `orynd_core/api/main.py` + `orynd_core/api/security.py`.
- ✅ 2.3 **ЧАСТЬ 2 — СПИСАНИЕ ✅ СДЕЛАНО (15.07).**
      **Точка списания найдена одна:** `_build_workspace_events()` (chat.py:298) — единственная воронка,
      через которую идут ВСЕ сборки (5 вызовов: repeat / assembly / complex_part / gear / primitive).
      Списываем там → покрыты все пути одним куском кода, без дублей.
      **Где именно:** ПОСЛЕ `compile_to_coreops` (значит turn реально строит) и **ДО `Pipeline([CADAgent()])`**
      (тяжёлая работа не запускается для юзера с нулём). Разговор/уточнения компилируются в 0 операций и
      выходят выше — **бесплатно, списание даже не вызывается** (проверено).
      **Fail-CLOSED:** Supabase недоступен → НЕ строим (`billing_unavailable`). Иначе лимит был бы
      рекомендацией: положил бы биллинг — строй сколько хочешь.
      **`spend_generation` RPC**: free_demos_left → потом credits_balance, атомарно, пишет ledger. Ответ
      разбирается и как dict, и как list (PostgREST заворачивает скалярный возврат) — обе формы проверены.
      **Проверено (моками, e2e будет в 3.3):** нет кредитов → NDJSON `{"type":"error","message":"No builds
      left — upgrade at oryndai.com"}` + **CAD запусков 0** ✅ · есть кредиты → RPC ровно 1 вызов + CAD пошёл ✅
      · разговор → 0 списаний ✅ · Supabase down → отказ ✅. **Тесты: 285 passed, 1 pre-existing env-fail.**
      🟡 НЕ сделано (осознанно, в roadmap): умный подсчёт дубликатов («5 колёс = 2 токена не 10») — требует
      распознавания идентичных элементов в оркестраторе, к релизу не успеваем. Сейчас: 1 сборка = 1 списание.
      ⚠️ **Файлы в деплой 3.2:** `orynd_core/routers/chat.py` (+ main.py, security.py из части 1).
      (макрос/поиск/декомпозиция), в т.ч. BYOK — списываем на КАЖДУЮ деталь, НЕ только route=ORYND.
      chat.py: если turn реально СТРОИТ/перестраивает деталь → RPC spend_generation(user_id из JWT) через
      supabase-хелпер (как billing.py) ДО тяжёлой работы; ok:false → NDJSON error «No builds left — upgrade
      at oryndai.com» и стоп. НЕ списывать на разговор/уточняющие вопросы/мелкие правки (founder: «уточняющие
      можно 100% задавать», не штрафуем за итерацию). ⚠️ Локальный макрос тоже гейтится подпиской (app
      phone-home перед сборкой — локальный compute напрямую не метрим, но приложение без подписки не строит).
      🟡 Умный подсчёт дубликатов (founder «5 колёс = 2 токена не 10») — оркестратор распознаёт идентичные
      элементы, списывает за уникальные, не за копии. Правило доработать (может уйти в roadmap если сложно к релизу).
      ⚠️ Предусловие: env Supabase на Вирджинии (Блок 3.1).
- ⬜ 2.4 **Остаток + подписка ВИДНЫ в приложении** (founder 15.07: «у нас локально сейчас даже нету ничего —
      человек не может посмотреть, сколько у него токенов, и какая у него подписка». Сделать ДО тестов):
      • bridge GET `/api/credits` → форвард на `api.oryndai.com/api/billing/me` с JWT юзера →
        `{plan, credits_balance, free_demos_left}`.
      • Счётчик в шапке = **одно число** (free_demos_left + credits_balance), без разбивки на бонусы
        (решение по бонусам: `knowledge_base/06_product/CONCEPT_token_system_bonuses.md`). Правки Header
        уже сделаны — заменить источник числа с mock на реальный; mock = offline-fallback.
      • **План юзера — реальный, не хардкод**: сейчас AccountMenu показывает зашитое «Pro · trial»
        (cad-auth.jsx:146) и dashboard «PRO · trial» (cad-dashboard.jsx:139), а `user.plan` дефолтится в
        'Free' — всё фейк. Подставить реальный plan из `/api/billing/me` в шапку/аккаунт-меню/Settings-бейдж.
      • Pro показываем как «Unlimited» (кап 60 не светим), Starter/Free — остаток числом.
- ✅ 2.5 **Share ✅ СДЕЛАНО (15.07).** `M_SHARE` (cad-popups.jsx, пресет к существующему CenterModal) →
      «This will be public: копируем одну строку + ссылку, файлы остаются на машине, мы ничего не
      заливаем» → clipboard `Made this with ORYND in {N}s — https://oryndai.com/try?utm_source=share`
      → toast. Кнопка Share — в шапке ран-блока (`RunProgress`, cad-thinking.jsx), появляется ровно
      при `!error && filesDir` (на провале и на разговоре её нет). `utm_source=share` совпадает с тем,
      что читает try.html (1.9) → шаринг трассируется до регистрации.
      ⚠️ Своё: `copyText()` с fallback на `execCommand` — панель живёт в ДВУХ движках (Chromium
      Electron'а и вебвью самого CAD'а), `navigator.clipboard` есть не везде; если и fallback не
      сработал — честный toast «clipboard is blocked in this pane», а не молчание.
- ⬜ 2.6 Project 0 (F1 локально) — **НЕ НАЧАТ, заблокирован дважды (15.07):** (1) нет STL — папки
      `app/assets/project0/` не существует вообще; (2) твоё же правило «СНАЧАЛА визуал → founder смотрит
      → ПОТОМ функционал» — делать вслепую нельзя. Панель онбординга (`cad-onboarding-f1.jsx`, чекофф по
      точному совпадению) уже живёт в приложении; синтез ран-событий + копия STL — не сделаны.
      UX по описанию founder'а:
      • Новичку при входе появляется ТЕСТОВОЕ окно (обзор «как это работает», как туториал у всех) — можно
        Skip, можно пройти. В окне сказано «сейчас сделаем F1», дальше юзер копирует каждый промпт.
      • Вся F1-история живёт КОНКРЕТНО в ЭТОМ чате/сессии. Новый чат — её нет; юзер может вернуться в
        прошлый чат и использовать (или забить). → Project 0 = отдельная chat-сессия, не глобальный стейт.
      • ⚠️ Порядок работы (founder): СНАЧАЛА показать, как я хочу чтобы это выглядело (визуал) → founder
        смотрит → ПОТОМ функционально подключаем. Не делать функционал вслепую.
      • Тех.реализация: 6 low-poly STL (лицензии CC) → `app/assets/project0/`; send() при совпадении с
        F1_PARTS.prompt НЕ фетчит бэкенд — синтез ран-событий (засечь ~15с для правдоподобия) + копия STL
        в ~/Documents/ORYND/project0/ → filesDir → чекофф как сейчас. Данные берутся из локального проекта.
        Проверить STL-импорт аддина (ORYNDFusion360AddIn.py) — нет → v1 сохранён+показан в Finder (не блокер).
- ✅ 2.7 **Мелочи ✅ СДЕЛАНО (15.07).**
      • Лейбл `search_models` → «Searching 50+ sources…» (cad-app.jsx:41).
      • **Диалог/ошибка — не сырой JSON.** Нашлась причина: мост писал заголовок 200+NDJSON и лил тело
        ЛЮБОГО ответа насквозь → при 401 (а гейт 2.3 их теперь и выдаёт!) в чат приезжал
        `{"detail":"Not authenticated"}`, UI такую строку не понимает → юзер видел «No response from
        backend». Теперь `streamChat` смотрит `statusCode` и отдаёт человеческий `{"type":"error"}`:
        401/403 → «Your session expired — sign out and sign in again», 402 → «No builds left — upgrade
        at oryndai.com», 429 → «Too many requests…», 5xx → «ORYND backend error (500) — …Settings ›
        Feedback». Слова бэкенда сохраняем, только если это уже предложение, а не JSON-дамп.
      • **Welcome-уведомление** — toast `T_WELCOME(n)` при первом входе, **цифра из Supabase**, не
        зашитая тройка (аккаунту, который уже потратил сборку, «3» была бы враньём). Молчит на платном
        плане (в шапке и так «Unlimited») и на нуле (в шапке и так «upgrade»). Один раз на установку
        (`localStorage: orynd-welcomed`). Текст: «The counter up top drops with every part you build.
        Questions and edits are free» — ровно твоя мысль «цифра будет меняться».
      ⚠️ Попутно: ещё в ДВУХ местах текст обещал, что ключ уезжает к нам на хранение («Sent securely to
        your ORYND backend session») — после 2.2 это ложь. Переписано в Settings и в LlmGate.
- ✅ 2.8 **Feedback ✅ СДЕЛАНО в коде (15.07). Тесты: 6/6.** ⬜ Ждёт founder'а: применить миграцию
      `20260715000001_feedback.sql` (без таблицы insert упадёт → юзер увидит «Could not send… try again»).
      • (а) **Секция Feedback в живых настройках** (`SettingsFull` — это `SETTINGS_VARIANT='full'`, т.е.
        то, что реально рендерится; в v2 не клал): поле + Send + состояния sending/sent/error. Подпись
        честная: «Sent: your message, your account, and the app version. Not your models, chats, or logs».
      • (б) **Всплывашка**: `T_FEEDBACK` с action-кнопкой, живёт 20с (у остальных тостов 4с — добавил
        `ttl`), клик → открывает Settings **сразу на Feedback** (`initialSection`), не «иди ищи».
        Правило показа моё (твоё «периодически / не назойливо» — компромисс): **после ВТОРОЙ успешной
        сборки, один раз за запуск приложения**. Просить фидбек до первого успеха или после провала —
        тон-глухо. Легко правится, если хочешь чаще.
      🚨 **Отклонение от плана (важное, про безопасность).** План говорил «bridge POST /api/feedback →
        insert в Supabase **service-ключом**». Так нельзя: мост — ЛОКАЛЬНЫЙ процесс на машине юзера,
        service-ключ обходит RLS → он уехал бы в каждый DMG = полный доступ к БД у любого, кто скачал
        приложение. Сделано: мост форвардит с JWT → **новый роутер `orynd_core/routers/feedback.py`**
        (`POST /api/feedback`, `Depends(current_user)`) пишет строку service-ключом на серваке.
      ⚠️ **Файлы в деплой 3.2 (НОВОЕ):** `orynd_core/routers/feedback.py` (новый файл) +
        `orynd_core/api/main.py` (уже в списке — там include_router).
- 🔄 2.9 **Верификация блока — сделано всё, кроме Project 0 (он в 2.6, ждёт STL).**
      • `pytest tests -q` → **300 passed, 1 failed** (тот же pre-existing env-fail library). Было 285:
        +9 тестов изоляции ключей (2.2), +6 тестов feedback (2.8).
      • `node --check` ✅: bridge.js + 6 затронутых jsx (cad-app, cad-popups, cad-screens, cad-thinking,
        cad-settings-full, cad-settings-v2).
      • 🆕 **Живой прогон моста, а не только синтаксис** — `cd app && npm run check:bridge` → **19/19**.
        Новый файл `app/test/bridge_check.js`: поднимает НАСТОЯЩИЙ мост на порту **8901** (не 8765 —
        твоё приложение не трогаю) против стаб-бэкенда. Доказано на самом деле, не на словах:
        нет ключа → заголовков нет · ключ едет `X-LLM-Key/Provider/Model` на каждый /chat · бэкенд
        видит ключ РОВНО ОДИН раз (валидация) · смена модели вообще не ходит на сервер · `/api/llm-status`
        показывает ключ подключённым, хотя стаб-бэкенд отвечает «ключей нет» (= правда моста, не сервера)
        · ключ не отражается наружу ни одним эндпоинтом · 401 → человеческая ошибка, а не `{"detail":…}`
        · feedback: аноним отбит, с JWT уходит текст+версия. (`!test` в electron-builder → в DMG не едет.)
      • ⬜ Офлайн-прогон Project 0 — только после STL (2.6).

# БЛОК 3 — Деплой + Тесты + Релиз ⬜
**Цель:** всё на проде, полный e2e живьём (тестируем И чиним на месте — сервак+локалка вместе), релиз v1.0.
**Закрыт когда:** founder прошёл полный путь в Fusion, тег v1.0.0 запушен, Actions собрал установщики,
инструкция на сайте.

## Таски
- ⬜ 3.1 Предпроверка: `ssh ec2-user@3.86.214.175 'systemctl show orynd-backend -p Environment'` —
      есть ли SUPABASE_URL/service key (нужно для 2.3). Нет → founder добавляет в systemd env.
- ⬜ 3.2 **Founder** деплой extension-бэкенда (Вирджиния): scp `services/llm/routes.py routers/chat.py
      agents/workspace.py routers/mcp.py routers/events.py agents/chat.py` → restart (полная команда:
      HANDOFF_2026-07-14_continue_here.md §Деплой). Проверка: `curl :8765/llm/status | jq .routes` → Gemini/Groq есть.
      🆕 **ДОБАВИЛОСЬ 15.07 (security + billing) — без этих файлов гейта и списания в проде НЕТ:**
      • `orynd_core/api/main.py` — Phase-2 auth-гейт (был log-only, пропускал всех) + include_router(feedback)
      • `orynd_core/api/security.py` — `GATED_PREFIXES` + `auth_gate_enabled()` (fail-closed)
      • `orynd_core/routers/chat.py` — списание `spend_generation` перед сборкой (уже в списке выше)
      🆕 **ДОБАВИЛОСЬ 15.07 (Блок 2):**
      • `orynd_core/routers/feedback.py` — **НОВЫЙ файл** (2.8). Без него `/api/feedback` = 404 и кнопка
        Send в настройках всегда ошибка. Нужна ещё таблица `feedback` в Supabase (миграция 1.11).
      • `orynd_core/services/llm/routes.py`, `orynd_core/agents/workspace.py` — изоляция ключей 2.2
        (оба УЖЕ в списке scp выше — просто не пропустить, иначе ключ юзера снова осядет в env процесса).
      🆕 **env на Вирджинии:** `ORYND_MCP_REQUIRE_AUTH=1` (закрыть открытый /mcp) + убедиться, что есть
      `SUPABASE_URL` + service key (нужно для списания — это предпроверка 3.1) + **НЕ ставить**
      `ORYND_DISABLE_AUTH_GATE` (в проде игнорируется, но не заводить его вообще).
      🆕 **Проверка гейта живьём после деплоя:** `curl -s -o /dev/null -w '%{http_code}' -X POST
      https://api.oryndai.com/chat -d '{"message":"x"}' -H 'Content-Type: application/json'` → **401**.
      Если 200 — гейт не доехал.
      ⚠️ **Бэкенд САЙТА — отдельный сервер (Стокгольм), другой деплой:** `orynd_core/routers/billing.py` +
      `orynd_core/auth/users.py` (там 5 багов с деньгами) → subtree push в `orynd_cl_site_profe_back` +
      на серваке `git pull && restart`. Env: `PLAN_STARTER_PRICE_USD=9.99`, `PLAN_STARTER_CREDITS=20`,
      `PLAN_PRO_CREDITS=60`. **Не перепутать с Вирджинией.**
- ⬜ 3.3 E2E цепочка исполнения (Claude через ORYND MCP-инструменты + curl; чиним сразу на месте).
      💡 Стратегия (founder): ключ-режим и MCP-режим почти идентичны (MCP идёт через команды) → сначала
      доводим до идеала через MCP (тесты почти полностью автоматизируются Claude'ом, founder не нужен),
      ПОТОМ проверяем через ключ. Founder нужен только: выдать себе токен (Supabase/ручками) + Fusion-live (3.5).
      • ключ-режим: Gemini-ключ → «cylinder radius 20 height 30» → ответ пришёл? файл (STL/STEP) создан?
        **открывается ли сразу? макрос запускается? как исполняется?** — смотреть весь путь до Fusion;
      • MCP-режим: вызов инструмента → mcp.step события → плашка в UI;
      • enforcement: юзер с 0 → честная ошибка на ЛЮБУЮ деталь (вкл. BYOK — подписка нужна всегда);
        счётчик в шапке декрементится на каждую деталь; разговор/уточнения НЕ списывают;
      • изоляция: две сессии/два ключа не пересекаются;
      • Project 0: 6/6 промптов, файлы открываются;
      • assist/сборка (0.1d, founder просил): добавить 2 детали → «собери сборку»/«соедини X с Y» → наш AI
        оркестрирует (координаты + assembly-команды). Работает — отлично; нет — фиксируем в roadmap, не блокер.
- ⬜ 3.4 **Чистый аккаунт с нуля** (founder дословно: «регистрируем новый аккаунт, проверяем, что всё то
      же самое работает, что запросы нормально уходят, что первый F1-опыт тоже работает»): новая
      регистрация → free-подписка авто-выдана (`ensure_subscription` в billing.py; free_demos_left=3 по
      новому дефолту из 1.2) → онбординг → Project 0 → обычный промпт → счётчик декрементится → всё как у
      существующего юзера.
- ⬜ 3.5 **Founder вживую**: Fusion open → промпт → деталь строится в Fusion, дерево видно, файл открылся.
- ⬜ 3.6 Чистка: убрать `_dlog`/`/api/debug-log` (bridge.js, cad-app.jsx, ORYNDFusion360AddIn.py).
- ⬜ 3.7 Коммит ВСЕГО (extension-репо + orynd_core локально; решение founder'а — коммит после зелёного e2e)
      → **founder**: `git push origin master && git tag v1.0.0 && git push origin v1.0.0` → Actions → DMG/EXE.
- ⬜ 3.8 Инструкция установки НА САЙТЕ: download.html секция «How to install» по платформам (Fusion: DMG →
      Scripts and Add-Ins → Run; Windows/EXE) — скрины founder'а, вёрстка Claude; + INSTALL.md в репо.
      (это второй маленький пуш сайта — можно вместе с 1.10, если скрины будут раньше)
- ⬜ 3.9 Замер качества/скорости (первично): прогнать разные запросы, засечь время (ждём ~15с/деталь),
      понять какие проходят хорошо/плохо. Не чинить сейчас — просто зафиксировать baseline.

# БЛОК 4 — Тест billing на РЕЛИЗНОЙ версии ⬜ (последняя проверка перед объявлением)
**Цель (словами founder'а):** «скачиваю релизную версию, создаю новый аккаунт, регистрируюсь, тестирую
что всё работает у бесплатного тарифа, покупаю (временно меняем цену) платный тариф, тестирую — вижу что
деньги пришли, подписка активировалась, тестирую платные функции, лимиты фиксируются/меняются, тестируем
на безопасность что обмануть нельзя». Делаем на СОБРАННОМ релизе (нужен установленный app + реальный
платёж), но ДО активного маркетинга/объявления.
- ⬜ 4.1 Скачать релизный DMG/EXE (из Actions), установить с нуля, зарегистрировать новый аккаунт.
- ⬜ 4.2 Free-тариф: 3 бесплатные детали есть, счётчик уменьшается, на 0 → paywall на ЛЮБУЮ деталь
      (вкл. BYOK — подписка нужна всегда). Разговор/уточнения не списывают.
- ⬜ 4.3 Оплата: временно снизить цену через Supabase `config` (сайт+бэкенд читают динамически) → купить
      Starter или Pro реальной криптой → CryptoBot webhook → деньги пришли, `payments` → paid,
      подписка активировалась, кредиты начислились → вернуть цену.
- ⬜ 4.4 Платные функции + лимиты: проверить что лимит реально фиксируется и меняется после генераций.
- ⬜ 4.5 **Безопасность (founder: обмануть нельзя):** попытаться обойти лимит с клиента (подделать баланс/
      вызвать списание/начисление напрямую) → должно быть невозможно (spend_generation только service_role,
      начисление только через webhook с валидной подписью). Это финальный гейт перед маркетингом.

---

## Отложено после релиза (не потеряется)
- 🔔 **MCP quick-install на сайте — НАПОМНИТЬ FOUNDER'У** (сказал 15.07: «запиши в план мб потом будем
  реализовывать, только напомни потом»). Сейчас быстрая установка через MCP — внизу страницы; хотим ВЫШЕ.
  **Его словами:**
  > «принцип, что нам надо максимально быстро, как можно быстрее сделать так, чтобы пользователь захотел
  > как минимум установить себе это MCP. Для этого самый лучший путь — это на вторую часть страницы,
  > практически после, ну, во втором блоке "Build the way you think", добавить быстро красивый визуал того
  > типа, как тебе быстро установить. У конкурентов посмотрим, как тебе быстро установить MCP.»
  > «есть два основных пути. Либо через коннекторы — то есть показывать весь путь: тебе надо зайти в Cloud,
  > нажать Connect, ещё что-то. А есть самый простой метод — это команда для Cloud. И вот это ещё проще
  > метод. Ты просто там оставляешь команду, говоришь "this command for Cloud", или "we are friendly to
  > Cloud", и он сам устанавливает коннектор.»
  > «дальше, после того как он установил коннектор… прикол этого коннектора в том, что коннектор
  > установлен. Ты спрашиваешь какой функционал условно — и прям можно одну маленькую команду сразу после
  > этого установочного кинуть, на сайте написать, чтобы чувак сразу мог понять как этим пользоваться. Ну
  > или типа команда help условно.»
  > «прикол в чём? Просто прикол в том, что таким образом чувак узнает: смотри, тебе чтобы пользоваться
  > этой MCPшкой, тебе нужно теперь download сделать, скачать вот эту историю, регистрируешься быстро. То
  > есть **мне не надо по действиям человеку объяснять — ему сам Cloud объяснит по действиям**: что он
  > сначала download делает; чтобы сделать download — заходит на сайт по этой ссылке, даёт ему ссылку,
  > сразу же на Sign in, говорит "зарегистрируйся", он регистрируется. Потом говорит "зайди под Cloud по
  > этой ссылке, download, скачиваешь". Потом в приложение заходишь под тем же самым аккаунтом — всё, тебе
  > выдаются бесплатные кредиты, можешь пройти обучение, и дальше переходить ко мне.»
  > «по факту ты на сайте две команды пишешь. Первую команду ключевую для установки — её даже можно с сайта
  > скопировать с красивым индикатором, либо просто текстом, без разницы честно. И дальше уже небольшая
  > команда для help.»
  **📌 РЕФЕРЕНС (founder прислал 15.07): `@thesapientcompany/mcp`** — «аналог хороший, качественный».
  Их блок: лейбл `ONE-COMMAND INSTALL` → рамка с `$ npx -y @thesapientcompany/mcp` + кнопка `COPY` →
  подпись «No account needed to try it — add a key to scan your own» → кнопка `READ THE API DOCS →`.
  Founder: «условно та же самая строка, только наше название вписать, нашу MCP-шку, и всё, и так же типа
  Send Cloud this, и добавить в начале типа **Download this connector**, или просто Download. Надо
  посмотреть, насколько строка большая будет получаться. И чувак берёт такой: о, классно, MCP-шка,
  копирует, кидает к Cloud, он устанавливает её».

  **❓ Вопрос founder'а: «мы же нигде не сможем подтянуть информацию, чтобы Cloud сам объяснил, как
  пользователю работать с этим. Или можем?»**
  **✅ ОТВЕТ: МОЖЕМ. И это УЖЕ реализовано** — проверено 15.07 в коде.
  • MCP-протокол на `initialize` возвращает поле `instructions`, и клиент (Claude) их читает и им следует.
    У нас это `orynd_core/routers/mcp.py:132`. Сейчас там технический текст для уже настроенного юзера:
    «ORYND engineering workspace tools: search 3D models, decompose meshes… Typical chain: macro_parse →
    cad_execute → standards_check». Это НЕ воронка.
  • **Значит `/help`-команда НЕ нужна и отдельный сайт с API НЕ нужен** (оба варианта founder рассматривал).
    Достаточно переписать `instructions` в воронку: «если у юзера нет аккаунта/приложения — проведи его:
    1) download → 2) sign in → 3) войти в приложение тем же аккаунтом → 4) выдаются free-кредиты → 5) можно
    строить». Claude сам расскажет по шагам — ровно то, чего founder и хотел («мне не надо по действиям
    человеку объяснять, ему сам Cloud объяснит по действиям»).
  • Усилить можно ответами инструментов: нет аккаунта/кредитов → tool result сам говорит, куда идти.
  • ⚠️ Т.е. на сайте нужна ОДНА команда (установка), вторая (help) — опциональна. Это и строку укоротит.

  **Техрасшифровка (что это значит в реализации):**
  • Новый блок на index.html сразу после hero-секции «Build the way you think» (сейчас MCP живёт внизу).
  • Два copy-блока: (1) команда установки коннектора для Claude, (2) команда help/«что умеешь».
  • Copy-to-clipboard с индикатором (как у конкурентов — посмотреть их подачу перед вёрсткой).
  • Суть воронки: **Claude сам ведёт юзера по шагам** (download → sign in → app → free credits →
    онбординг), нам не надо расписывать инструкцию — экономит объяснения и снижает трение до нуля.
  • Зависимость: `/mcp` на проде сейчас открыт в интернет (см. AWS-заметку) — гнать трафик на MCP ДО
    закрытия гейта нельзя. Сначала `ORYND_MCP_REQUIRE_AUTH=1` + деплой, потом промо MCP.
- **Бонус-токены / механики выдачи кредитов** — концепт готов, НЕ реализовано, в Supabase ничего не заведено:
  `knowledge_base/06_product/CONCEPT_token_system_bonuses.md`. Тайминг: **не раньше ~1000 юзеров** (решение
  founder'а). Правила: выдача только вручную (иначе фарм), привязка к НАШЕМУ событию-релизу (стена твёрдая),
  подарок = разовое уведомление с причиной, не ярлык. Работы ~45 мин, когда дойдёт.
- Search-апгрейд: отдельный брейншторм-сессия — rating по звёздам/отзывам, выбор лучшего БЕЗ превью
  (решение founder'а), fan-out по 50 источникам, Perplexity/Exa опционально. Сейчас реально работает
  только Thingiverse API. Начало: перечитать этот блок + orynd_core/agents/retrieval.py.
- Team-план: рассинхрон $60 (account.html) / $99 (billing.py) — выровнять.
- Concurrency: 1 uvicorn-процесс, тяжёлый CAD последовательный → воркеры/очередь. 100 параллельных не держит.
- UI rewrite — только по команде founder'а.

## Известные баги Fusion (из хендоффа 14.07, НЕ блокеры релиза — если вылезут в 3.5, чинить там)
1. Панель ORYND пропадает, когда начинаешь создавать деталь в Fusion (гипотеза: смена workspace/таба
   рвёт связь с палитрой). Founder-наблюдение, не расследовано.
2. Краш Python-аддина внутри Fusion не виден в UI — статус зависает на «Connecting» навсегда, канала
   об ошибке нет.
- Отложенная фича «одно окно»: Fusion открыт → главное Electron-окно прячется в трей, панель = основной
  интерфейс (частично умеет: window-all-closed не квитает процесс).

## Решения founder'а (зафиксированы 14-15.07)
3 плана free/starter $9.99/pro $20 · free = 3 генерации (free_demos_left), цифры на сайте не озвучивать ·
серверный enforcement обязателен + ручная правка баланса остаётся · цены на сайте из Supabase · F1 =
локально «Project 0» в отдельном чате, визуал показать founder'у до функционала · Share V1 = текст+ссылка ·
превью в поиске не делать · инструкция на сайте · коммит только после полного e2e · авто-установка в CAD —
отклонена · план = блоки, можно по блоку на сессию.
✅ Модель токенов РЕШЕНА (Блок 0, 15.07): 3 триала → $10 Starter → $20 Pro; подписка нужна всегда (BYOK не
обход); макросы платные; деталь=единица, разговор бесплатен; списание на каждую деталь; Pro-кап ≥100.
Макросы платные, decompose=редактируемый STEP (дифференциатор) — подтверждено.

## Открытые вопросы founder'у
- (закрыто) модель токенов — решена.
- (закрыто 15.07) **Starter = 20 деталей/мес** (founder выбрал 20 из вилки 15/20/25). Код: fallback в
  billing.py `PLAN_STARTER_CREDITS=20` ✅. **В Supabase `config.starter_generations=20`** ✅ — founder
  применил SQL 15.07, БД и код сошлись.
- (закрыто 15.07) **«3-day free trial» → «3 free builds» ВЕЗДЕ** (founder): сайт продавал 3-дневный триал
  (~20 мест), а модель = триал по счёту деталей без срока. Сайт приводим к тому, что реально делает код.
- (закрыто 15.07) **Watermark/preview-ограничение убрать из текста** (founder): в коде watermark'а нет,
  free-юзер получает полные файлы. Free = 3 полные детали, полный экспорт.
