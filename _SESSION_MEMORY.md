# SESSION MEMORY — ORYND CAD Bridge (Extension)

> **Старт-промпт RELEASE DAY** (вставь в новый чат; вместо N подставь номер блока 1/2/3):
> ```
> Релиз-день ORYND Extension. Работаем СТРОГО по плану, НЕ пересобирай контекст заново:
> прочитай dist_public/orynd_cad_bridge/ACTIVE_TASK.md — там 3 самодостаточных блока с вшитым
> контекстом. Сейчас закрываем БЛОК N. Прочитай его контекст и таски, выполняй по порядку,
> отмечай чекбоксы в файле. Также глянь _SESSION_MEMORY.md Progress Log (что уже сделано).
> Правила: НИКОГДА git push из корня ORYND_Workspace или orynd_core; сайт = orynd_site_v3/ →
> пуш ТОЛЬКО в static-v3, коммиты от автора Sfnbinib (SaveliTor Vercel блокирует); НЕ рестартовать
> моё запущенное приложение/бэкенд без спроса; Supabase через MCP (если не подключён — скажи мне
> активировать); деплой на серваки и пуши в прод — я руками по твоим командам.
> Состояние working tree: НЕзакоммичено orynd_core (BYOK Фаза A) + extension (6 файлов) —
> осознанно, коммит после e2e в Блоке 3. Вопросы задавай, если что-то противоречит плану.
> ```

## Project Snapshot
- **Продукт:** Electron-панель для CAD (сейчас Fusion 360), отдельный git-репо
  (`dist_public/orynd_cad_bridge`, origin → `extension-oryndai-solid-app`), НЕ Workspace.
- **Бэкенд:** `orynd_core/` (общий с Workspace), FastAPI, порт 8765.
- **Текущий этап:** Stage 1 — V1 Launch baseline. План: `ACTIVE_TASK.md`.

## Правило работы (см. MASTER_PLAN.md «Правило работы»)
Прогресс пишем В ЭТИ файлы (ACTIVE_TASK.md чекбоксы + этот Progress Log), не в новые дата-файлы.
Новый `HANDOFF_*`/`PLAN_*` в `knowledge_base/01_sessions/` — только как архивная запись ПОСЛЕ
закрытия этапа, никогда как замена активному плану.

## Progress Log

### 2026-07-14 — Stage 1 запущен, Фаза A (BYOK) закрыта
- Диагностирован и закрыт корень 401: фронт слал ключ без `provider`, Gemini-ключ уходил как
  Anthropic → отвергался → 401 при генерации. Backend уже был мульти-провайдер (routes.py) —
  фикс в основном фронтовый + добавление gemini/groq routes.
- Реализовано: авто-детект провайдера по префиксу ключа + override, живой дропдаун моделей
  (умный дефолт, юзеру выбирать не надо), ORYND-дефолт использует любой присутствующий ключ.
- Founder-корректировка: убран "Local model" route и упоминания Ollama из Extension UI —
  Extension архитектурно не имеет локальной модели (это Workspace-слой), BYOK-only.
- Верификация: 6 TestClient e2e-чеков + 285 pytest + node --check фронта. Живой Electron-прогон
  с реальным ключом — за founder'ом.
- **Founder-методология (тот же день):** этот трёхфайловый канон (MASTER_PLAN/ACTIVE_TASK/
  _SESSION_MEMORY) создан для Extension впервые — раньше работа шла через накапливающиеся
  датированные HANDOFF_*/PLAN_*.md в knowledge_base/01_sessions без единого текущего состояния.
  Причина: founder заметил, что сессии не имеют линии преемственности. Впредь: один живой план
  на этап, архивные файлы — только на закрытии этапа.

### 2026-07-15 — Концепция цен зафиксирована + Supabase-часть Блока 1 ✅
- Новая модель: подписка = кредиты = генерации. Free 3 / Starter $9.99 (~15) / Pro $20 (кап 30, показываем
  «Unlimited»). Кредит списывается только на route=ORYND (BYOK — бесплатно). Полная концепция + схема
  связи сайт↔Supabase↔бэкенды — в ACTIVE_TASK.md Блок 1 «КОНЦЕПЦИЯ».
- **Supabase сделан через MCP** (коннектор был только в той сессии): config-цены+display, RPC
  spend_generation (service_role only — закрыл дыру, что нашёл адвайзор), free_demos_left default→3,
  все free-юзеры выровнены. Точное состояние = ACTIVE_TASK Блок 1 «SUPABASE — СДЕЛАНО». Новой сессии
  БЕЗ Supabase-коннектора: состояние уже описано там, фронт подключается по нему, повторно лезть в
  Supabase не нужно (кроме реальной оплаты в Блоке 3).
- 🟡 ОТКРЫТО для founder'а: чем Starter отличается от Pro, если Pro=«Unlimited» (вопрос в Блоке 1).
- Осталось в Блоке 1: billing.py (starter-план), фронт (pricing.js + карточки + try.html), пуш — founder.

### 2026-07-15 (later) — Модель токенов РЕШЕНА + вижн разложен + Блок 0.1
- Токены РЕШЕНЫ (Блок 0): 3 триала → $10 Starter → $20 Pro; подписка нужна всегда (BYOK не обход);
  макросы платные; деталь=единица, разговор бесплатен; списание на каждую деталь; Pro-кап ≥100; free=3 (не растим).
- MecAgent проверен: они кредит-капнуты ($16/100, $84/200), не безлимит; их free 30/мес — мы идём иначе.
- Decompose ПОДТВЕРЖДЁН = редактируемый STEP (дифференциатор). Reverse-eng инструкция/сборка-оркестрация/assist —
  НЕ проверены, лежат check-пунктами в Блоке 0.1 + 3.3 (founder: не тратить токены на проверку сейчас).
- Добавлены: Блок 0.1 (аудит ядра), Блок 4 (тест billing), feedback (2.8), Project 0 UX (визуал до функционала),
  MCP-first тест-стратегия (3.3).
- **Вижн founder'а сохранён ДВУМЯ слоями:** дословно → `knowledge_base/06_product/FOUNDER_RAW_2026-07-15_*.md`
  (источник правды, founder распределит сам); дистиллят → ROADMAP_extension/site/workspace.md. Правило в ORYND_SYSTEM.md.

## Next Actions
1. Живой Electron-прогон Фазы A (Gemini-ключ, реальный запрос) — founder.
2. Фаза B: видимый счётчик кредитов + Share-кнопка (локальный код, следующий шаг Claude).
3. Site FRONT push (`FRONT 3 v2`, `git push origin master`, НЕ `profe`) — founder, независим от B.

## Decisions Log
- Extension и Workspace — раздельные продукты, раздельные git-репо, общий backend. Workspace
  не трогать без прямой команды (`knowledge_base/`-память: Extension not Workspace).
- BYOK-модель: ключ привязан к провайдеру, не к модели; модель выбирается живым списком с
  умным дефолтом, юзер не обязан выбирать вручную (founder-решение 14.07).
